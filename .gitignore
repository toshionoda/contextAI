# 環境・認証（リポジトリに含めない）
.env
.env.local

# Google OAuth（ローカル用。CI では Secrets 経由で注入）
token.json
credentials.json
*client_secret*.json
context/credentials*.json
context/*client_secret*.json
context/*.apps.googleusercontent.com.json

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
.venv/
venv/
.env.local

# 出力・キャッシュ（必要なら外す）
# mnt/
