# 環境・認証（リポジトリに含めない）
.env
.env.local

# Google OAuth（ローカル用。CI では Secrets 経由で注入）
token.json
credentials.json
*client_secret*.json
context/credentials*.json
context/*client_secret*.json
context/*.apps.googleusercontent.com.json

# OS
.DS_Store
Thumbs.db

# 大容量メディア（GitHub 100MB制限超過のため除外）
*.mp4
*.mov
*.avi
*.mkv

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
.venv/
venv/
.env.local

# 出力・キャッシュ（必要なら外す）
# mnt/
